{
  "exec-opts": [{{ DOCKER_OPTS | default('"native.cgroupdriver=cgroupfs"') }}],
  "registry-mirrors": {{ DOCKER_MIRROR_HOSTS }},
  "log-opts" : {
    "max-file" : "{{ DOCKER_LOG_MAX_FILE }}",
    "max-size" : "{{ DOCKER_LOG_MAX_SIZE }}"
  }
}
